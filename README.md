=>  In order to start this project, docker should be installed in the machine

=>  Once docker is installed just run the following command:

        docker-compose up

=>  Visit http://localhost:3000/